const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

// module.exports = { add, sub, multiply, divide };
// exported to server.js

// ES6 Modules import exports of Modules :
export { add, sub, multiply, divide };
