// -------------------------------------------------------------------------------------------------------
// normal JS const variable with function
const sum = (a, b) => {
  return a + b;
};

const diff = (a, b) => {
  return a - b;
};

// Export short hand
export { sum, diff };
